{% extends 'base.html.twig' %}

{% block title %}Mon profil{% endblock %}

{% block body %}

<h1>MON PROFIL</h1>

{% for article in articles %}

    <a href="{{ path("id_article", {"id" : article.id}) }}">{{ article.title }} - {{ article.content }}</a><br>

{% endfor %}

<br>
<a href="{{ path("manage", {"name" : user.email}) }}">Modifier profil</a>

{% endblock %}